{
  "name": "pitv-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.js --dev",
    "build": "node scripts/build.js",
    "check": "tsc --noEmit",
    "preview": "npx esbuild --serve=4173 --servedir=dist",
    "smoke": "node scripts/smoke-test.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.0.0"
  }
}
